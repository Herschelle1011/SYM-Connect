@* @model SYM_CONNECT.Models.Event

<form asp-action="Create" asp-controller="Events" method="post">
    @Html.AntiForgeryToken()

    <!-- Create Event Modal -->
    <div class="modal fade show d-block" id="createEventModal" tabindex="-1" aria-hidden="false" style="background:rgba(0,0,0,0.3);">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content" style="border-radius:16px; padding:24px; border:none;">

                <!-- Header -->
                <div class="modal-header border-0 p-0 mb-2">
                    <h5 class="modal-title fw-semibold" style="color:#00bcd4; font-size:1.3rem;">
                        <i class="bi bi-calendar-plus-fill"></i> Create Event
                    </h5>
                </div>
                <p style="color:#888; font-size:.85rem;">Fill out the form below to add a new event to the system.</p>

                <!-- Body -->
                <div class="modal-body p-0">
                    <div class="row g-3">

                        <!-- Title -->
                        <div class="col-12">
                            <input type="text" class="form-control" placeholder="Event Title" asp-for="Title" />
                            <span asp-validation-for="Title" class="text-danger" style="font-size:.75rem;"></span>
                        </div>

                        <!-- Description -->
                        <div class="col-12">
                            <textarea class="form-control" placeholder="Description" asp-for="Description" rows="3" style="resize:none;"></textarea>
                            <span asp-validation-for="Description" class="text-danger" style="font-size:.75rem;"></span>
                        </div>

                        <!-- Event Date -->
                        <div class="col-12">
                            <input type="datetime-local" class="form-control" asp-for="EventDate" />
                            <span asp-validation-for="EventDate" class="text-danger" style="font-size:.75rem;"></span>
                        </div>

                        <!-- Created By & Approval Status -->
                        <div class="col-6">
                            <label class="form-label">Created By</label>

                            <input class="form-control"
                                   value="@ViewBag.CurrentUserName"
                                   readonly />
                        </div>

                        <!-- Approval Status -->
                        <div class="col-6">
                            <label class="form-label">Approval Status</label>
                            <input class="form-control"
                                   value="Pending"
                                   readonly />
                        </div>


                        <!-- Approved By -->
                        <div class="col-12">
                            <label class="form-label">Approved By</label>
                                <input class="form-control"
                                       value="@(ViewBag.CurrentUserName ?? "Pending Approval")"
                                       readonly />
                        </div>
                    </div>
                </div>

                <!-- Footer -->
                <div class="modal-footer border-0 p-0 mt-4">
                    <a asp-action="Index" class="btn btn-secondary" style="text-decoration:none;">Cancel</a>
                    <button type="submit" class="btn btn-info text-white px-4">
                        Create
                    </button>
                </div>

            </div>
        </div>
    </div>

</form>

<script>
    // Re-open modal if validation errors exist
    document.addEventListener("DOMContentLoaded", function () {
        var hasErrors = document.querySelector(".field-validation-error");
        if (hasErrors) {
            var modal = new bootstrap.Modal(document.getElementById('createEventModal'));
            modal.show();
        }
    });

    function filterTable() {
        const input = document.getElementById('searchInput').value.toLowerCase();
        const rows = document.querySelectorAll('#tableBody tr');
        rows.forEach(row => {
            row.style.display = row.innerText.toLowerCase().includes(input) ? '' : 'none';
        });
    }
</script>
 *@
